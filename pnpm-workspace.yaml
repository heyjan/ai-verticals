packages:
  - db
  - dashboard
  - classifier

# Native/postinstall deps whose build scripts pnpm is allowed to run
# (pnpm blocks dependency lifecycle scripts by default).
allowBuilds:
  '@parcel/watcher': true
  better-sqlite3: true
  esbuild: true
  vue-demi: true
