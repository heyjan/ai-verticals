# Agents

## Git workflow

- Always create a new branch for changes. Never commit directly to `main`.
- After a branch is merged, delete it.

## Tech stack

- Use best practices for **Nuxt 4**, **Tailwind 4**, and **Three.js**.
- Always use the latest stable versions of all dependencies.
