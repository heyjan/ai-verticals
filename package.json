{
  "name": "ai-job-classifier",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "seed": "pnpm --filter @ai-job-classifier/db seed",
    "import": "pnpm --filter @ai-job-classifier/db import",
    "migrate": "pnpm --filter @ai-job-classifier/db migrate",
    "dev": "pnpm --filter ai-job-dashboard dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.1.7"
  }
}
